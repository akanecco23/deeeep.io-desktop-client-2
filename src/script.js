/** This is the main script **/

(() => {
	// Make sure script is running on Deeeep.io
	if (!window.location.hostname.match(/(beta\.|alpha\.)?deeeep\.io/) || !document.documentElement.outerHTML) return;

	// Do things...
})();
